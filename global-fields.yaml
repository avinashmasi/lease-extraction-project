kind: Ontology/v2
name: global-fields
namespaces:

  self:

    # jurisdiction
    jurisdiction:
      reduction:
        $precedence:
          - $get: jurisdiction
          - $const: en-us

    # validation
    validation:
      reduction:
        $precedence:
          - $get: validation
          - $const: fuzzy

    # response language
    response_language:
      reduction:
        $precedence:
          - $get: $response_language
          - $set: en  # en - english, es - spanish, jp - japanese, de - german, etc.

    # response encoding
    response_encoding:
      reduction:
        $precedence:
          - $get: $response_encoding
          - $eval:
              lang: =self.response_language
              expression: "'ascii' if lang == 'en' else 'utf-8'"
          - $set: 'ascii'

    # #################################
    # Terms

    Arrangement_Start_Date:
      reduction:
        $match:
          key: =self.jurisdiction
          default: =en-nl-completions-terms/self.Arrangement_Start_Date
          en-nl: =en-nl-completions-terms/self.Arrangement_Start_Date

    Arrangement_End_Date:
      reduction:
        $match:
          key: =self.jurisdiction
          default: =en-nl-completions-terms/self.Arrangement_End_Date
          en-nl: =en-nl-completions-terms/self.Arrangement_End_Date

    Occupancy_Date:
      reduction:
        $match:
          key: =self.jurisdiction
          default: =en-nl-completions-terms/self.Occupancy_Date
          en-nl: =en-nl-completions-terms/self.Occupancy_Date

    Lease_Term:
      reduction:
        $match:
          key: =self.jurisdiction
          default: =en-nl-completions-terms/self.Lease_Term
          en-nl: =en-nl-completions-terms/self.Lease_Term

    Ref_lease_term_uom_desc:
      reduction:
        $match:
          key: =self.jurisdiction
          default: =en-nl-completions-terms/self.Ref_lease_term_uom_desc
          en-nl: =en-nl-completions-terms/self.Ref_lease_term_uom_desc

    Reserved_Parking_Spaces:
      reduction:
        $match:
          key: =self.jurisdiction
          default: =en-nl-completions-terms/self.Reserved_Parking_Spaces
          en-nl: =en-nl-completions-terms/self.Reserved_Parking_Spaces

    Cost_per_Reserved_Parking_Space:
      reduction:
        $match:
          key: =self.jurisdiction
          default: =en-nl-completions-terms/self.Cost_per_Reserved_Parking_Space
          en-nl: =en-nl-completions-terms/self.Cost_per_Reserved_Parking_Space

    Ref_cost_per_reserved_parking_space_uom_desc:
      reduction:
        $match:
          key: =self.jurisdiction
          default: =en-nl-completions-terms/self.Ref_cost_per_reserved_parking_space_uom_desc
          en-nl: =en-nl-completions-terms/self.Ref_cost_per_reserved_parking_space_uom_desc

    Ref_property_space_quality_type_desc:
      reduction:
        $match:
          key: =self.jurisdiction
          default: =en-nl-completions-terms/self.Ref_property_space_quality_type_desc
          en-nl: =en-nl-completions-terms/self.Ref_property_space_quality_type_desc

    Arrangement_Signed_Date:
      reduction:
        $match:
          key: =self.jurisdiction
          default: =en-nl-completions-terms/self.Arrangement_Signed_Date
          en-nl: =en-nl-completions-terms/self.Arrangement_Signed_Date

    Asking_Rent_Monthly:
      reduction:
        $match:
          key: =self.jurisdiction
          default: =en-nl-completions-terms/self.Asking_Rent_Monthly
          en-nl: =en-nl-completions-terms/self.Asking_Rent_Monthly

    Ref_asking_rent_monthly_uom_desc:
      reduction:
        $match:
          key: =self.jurisdiction
          default: =en-nl-completions-terms/self.Ref_asking_rent_monthly_uom_desc
          en-nl: =en-nl-completions-terms/self.Ref_asking_rent_monthly_uom_desc

    Asking_Rent_Yearly:
      reduction:
        $match:
          key: =self.jurisdiction
          default: =en-nl-completions-terms/self.Asking_Rent_Yearly
          en-nl: =en-nl-completions-terms/self.Asking_Rent_Yearly

    Ref_asking_rent_yearly_uom_desc:
      reduction:
        $match:
          key: =self.jurisdiction
          default: =en-nl-completions-terms/self.Ref_asking_rent_yearly_uom_desc
          en-nl: =en-nl-completions-terms/self.Ref_asking_rent_yearly_uom_desc

    Effective_Rent_Monthly:
      reduction:
        $match:
          key: =self.jurisdiction
          default: =en-nl-completions-terms/self.Effective_Rent_Monthly
          en-nl: =en-nl-completions-terms/self.Effective_Rent_Monthly

    Ref_effective_rent_monthly_uom_desc:
      reduction:
        $match:
          key: =self.jurisdiction
          default: =en-nl-completions-terms/self.Ref_effective_rent_monthly_uom_desc
          en-nl: =en-nl-completions-terms/self.Ref_effective_rent_monthly_uom_desc

    Effective_Rent_Yearly:
      reduction:
        $match:
          key: =self.jurisdiction
          default: =en-nl-completions-terms/self.Effective_Rent_Yearly
          en-nl: =en-nl-completions-terms/self.Effective_Rent_Yearly

    Ref_effective_rent_yearly_uom_desc:
      reduction:
        $match:
          key: =self.jurisdiction
          default: =en-nl-completions-terms/self.Ref_effective_rent_yearly_uom_desc
          en-nl: =en-nl-completions-terms/self.Ref_effective_rent_yearly_uom_desc

    Service_Charge_Per_Area_Value:
      reduction:
        $match:
          key: =self.jurisdiction
          default: =en-nl-completions-terms/self.Service_Charge_Per_Area_Value
          en-nl: =en-nl-completions-terms/self.Service_Charge_Per_Area_Value

    Ref_service_charge_per_area_value_price_uom_desc:
      reduction:
        $match:
          key: =self.jurisdiction
          default: =en-nl-completions-terms/self.Ref_service_charge_per_area_value_price_uom_desc
          en-nl: =en-nl-completions-terms/self.Ref_service_charge_per_area_value_price_uom_desc

    Number_Of_Month_Free_Rent:
      reduction:
        $match:
          key: =self.jurisdiction
          default: =en-nl-completions-terms/self.Number_Of_Month_Free_Rent
          en-nl: =en-nl-completions-terms/self.Number_Of_Month_Free_Rent

    Tenant_Improvement_Allowance:
      reduction:
        $match:
          key: =self.jurisdiction
          default: =en-nl-completions-terms/self.Tenant_Improvement_Allowance
          en-nl: =en-nl-completions-terms/self.Tenant_Improvement_Allowance

    Ref_tenant_improvement_allowance_uom_desc:
      reduction:
        $match:
          key: =self.jurisdiction
          default: =en-nl-completions-terms/self.Ref_tenant_improvement_allowance_uom_desc
          en-nl: =en-nl-completions-terms/self.Ref_tenant_improvement_allowance_uom_desc

    Option_Notes:
      reduction:
        $match:
          key: =self.jurisdiction
          default: =en-nl-completions-terms/self.Option_Notes
          en-nl: =en-nl-completions-terms/self.Option_Notes

    
    # Contacts

    contact_display_name:
      reduction:
        $match:
          key: =self.jurisdiction
          default: =en-nl-completions-contacts/self.contact_display_name
          en-nl: =en-nl-completions-contacts/self.contact_display_name

    first_name:
      reduction:
        $match:
          key: =self.jurisdiction
          default: =en-nl-completions-contacts/self.first_name
          en-nl: =en-nl-completions-contacts/self.first_name

    last_name:
      reduction:
        $match:
          key: =self.jurisdiction
          default: =en-nl-completions-contacts/self.last_name
          en-nl: =en-nl-completions-contacts/self.last_name

    email:
      reduction:
        $match:
          key: =self.jurisdiction
          default: =en-nl-completions-contacts/self.email
          en-nl: =en-nl-completions-contacts/self.email

    full_phone_number:
      reduction:
        $match:
          key: =self.jurisdiction
          default: =en-nl-completions-contacts/self.full_phone_number
          en-nl: =en-nl-completions-contacts/self.full_phone_number

    # Lease comp details

    total_leased_space:
      reduction:
        $match:
          key: =self.jurisdiction
          default: =en-nl-completions-lease-comp-details/self.total_leased_space
          en-nl: =en-nl-completions-lease-comp-details/self.total_leased_space

    ref_total_leased_space_uom_desc:
      reduction:
        $match:
          key: =self.jurisdiction
          default: =en-nl-completions-lease-comp-details/self.ref_total_leased_space_uom_desc
          en-nl: =en-nl-completions-lease-comp-details/self.ref_total_leased_space_uom_desc

    ref_lease_activity_type_desc:
      reduction:
        $match:
          key: =self.jurisdiction
          default: =en-nl-completions-lease-comp-details/self.ref_lease_activity_type_desc
          en-nl: =en-nl-completions-lease-comp-details/self.ref_lease_activity_type_desc

    ref_lease_arrangement_type_desc:
      reduction:
        $match:
          key: =self.jurisdiction
          default: =en-nl-completions-lease-comp-details/self.ref_lease_arrangement_type_desc
          en-nl: =en-nl-completions-lease-comp-details/self.ref_lease_arrangement_type_desc

    # Property type parking details

    parking_cost_monthly:
      reduction:
        $match:
          key: =self.jurisdiction
          default: =en-nl-completions-property-type-parking-details/self.parking_cost_monthly
          en-nl: =en-nl-completions-property-type-parking-details/self.parking_cost_monthly

